name: Build MAME 0.139u1 (Final Success Mode)
on: [push]

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout your files
        uses: actions/checkout@v4

      - name: Setup Cache
        uses: actions/cache@v4
        with:
          path: mame-src/obj
          key: ${{ runner.os }}-mame-build-${{ github.run_id }}
          restore-keys: |
            ${{ runner.os }}-mame-build-

      - name: Extract and Clean Source
        shell: powershell
        run: |
          if (!(Test-Path "mame-src")) {
            Expand-Archive -Path ".github/workflows/mame0139s.zip" -DestinationPath "temp_folder"
            $innerZip = Get-ChildItem -Path "temp_folder" -Filter *.zip | Select-Object -First 1
            Expand-Archive -Path $innerZip.FullName -DestinationPath "mame-src"
            Copy-Item -Path "input_fix.cpp" -Destination "mame-src/src/osd/windows/" -Force
            Copy-Item -Path "net_sync.cpp" -Destination "mame-src/src/osd/windows/" -Force
          }
          # خراب فائلوں کو ہٹانا تاکہ بلڈ نہ رکے
          if (Test-Path "mame-src/src/mame/drivers/albazg.c") { Remove-Item "mame-src/src/mame/drivers/albazg.c" }
          if (Test-Path "mame-src/src/mame/drivers/kaneko16.c") { Remove-Item "mame-src/src/mame/drivers/kaneko16.c" }

      - name: Setup Compiler
        uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW32
          install: make mingw-w64-i686-gcc

      - name: Compile MAME (Ultimate)
        shell: msys2 {0}
        run: |
          cd mame-src
          export CFLAGS="-w"
          export CXXFLAGS="-w"
          # اب یہ ان فائلوں کو چھوڑ کر آگے بڑھ جائے گا
          make -j2 NOWERROR=1 SUBTARGET=mame || make -j1 NOWERROR=1 SUBTARGET=mame

      - name: Upload Final EXE
        uses: actions/upload-artifact@v4
        with:
          name: MAME-0139u1-Netplay-EXE
          path: |
            mame-src/*.exe
            mame-src/mame*.exe
