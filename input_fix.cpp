void sync_all_players() {
    for (auto& p : connected_players) {
        // Map each mobile IP to MAME Player Slots (P2, P3, P4)
        // Agar 3rd player join karega to use automatic P3 control mil jayega
    }
}